AMAZON_EMAIL=
AMAZON_PASSWORD=
ASIN=

# Needed off macOS, or when OCR_MODEL is set. macOS reads pages locally.
OPENAI_API_KEY=
# Leave empty to read pages on this Mac. Name an OpenAI model (for example
# gpt-4.1-mini) to have OpenAI read them instead, which needs the key above.
OCR_MODEL=
